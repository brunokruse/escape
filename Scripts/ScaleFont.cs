using UnityEngine;
using System.Collections;

public class ScaleFont : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	  	//Debug.Log (Time.time);
		
		//transform.localScale = new Vector3(Mathf.PingPong(Time.time, 1.0f) + 1.0f, Mathf.PingPong(Time.time, 1.0f) + 1.0f, 0.0f);
		//transform.Rotate = new Vector3(Mathf.PingPong(Time.time, 1.0f) + 1.0f, Mathf.PingPong(Time.time, 1.0f) + 1.0f, 0.0f);
		
	}
}
